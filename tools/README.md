# tools/

Binários locais **não versionados**.

## Atlassian CLI

```bash
pnpm acli:install
```

Isso grava `tools/acli.exe` (Windows) ou `tools/acli` (macOS/Linux).
Depois: `pnpm jira:auth` e `docs/ops/acli-jira.md`.

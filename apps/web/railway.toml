[build]
builder = "NIXPACKS"
# watch paths do Railway incluem apps/web — alterações na raiz (.npmrc) não disparam deploy

[deploy]
startCommand = "pnpm --filter @torcida/web start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
